apiVersion: apps/v1
kind: Deployment
metadata:
  name: scoring-service
  namespace: crickethub
spec:
  replicas: 1
  selector: { matchLabels: { app: scoring-service } }
  template:
    metadata: { labels: { app: scoring-service } }
    spec:
      containers:
        - name: scoring-service
          image: crickethub/scoring-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8084 }]
          env:
            - { name: PORT, value: "8084" }
            - { name: DB_HOST, value: "postgres" }
            - { name: DB_PORT, value: "5432" }
            - { name: DB_NAME, value: "scoring_db" }
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: postgres-secret, key: POSTGRES_USER } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-secret, key: POSTGRES_PASSWORD } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: postgres-secret, key: JWT_SECRET } }
          readinessProbe:
            httpGet: { path: /health, port: 8084 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8084 }
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata: { name: scoring-service, namespace: crickethub }
spec:
  selector: { app: scoring-service }
  ports: [{ port: 8084, targetPort: 8084 }]
